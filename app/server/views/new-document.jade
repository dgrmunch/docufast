- user = typeof(udata) != 'undefined' ? udata : { }

#account-form-container
	form( method="post")#document-form.form-horizontal.well.span6
		div.panel.panel-default
		h3 Create a document
		p After the process, it will be sent to the email address introduced bellow.
			fieldset
				input(type='hidden', value= user.user)#user
				#name-cg.control-group
					label.control-label(for='name-tf') Title
					.controls
						input#name-tf.input-xlarge(type='text', name='name', value= 'My document')
				#email-cg.control-group
					label.control-label(for='email-tf') Email
					.controls
						input#email-tf.input-xlarge(type='text', name='email', value= user.email)
			button(type="submit")#btn-list.btn.btn-success
				i.icon-ok.icon-white
				| Next